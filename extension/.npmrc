# Skip dependency install scripts. The only one is keytar's native build
# (a transitive dep of @vscode/vsce), needed solely for `vsce publish`, which
# we never run — distribution is GitHub Releases. Explicit `npm run` scripts
# (compile, package) are unaffected.
ignore-scripts=true
